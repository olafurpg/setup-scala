name: CI
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - run: npm ci
    - run: npm run build
    - uses: ./
    - name: Test sbt build
      run: |
        cd test-build
        sbt run
